# Pipeline

The `unicef-security` app provides custom social authentication pipeline functions.

## social_details

A pipeline function that adds the user's IDP to the details.

## get_username

A pipeline function that gets the user's username from their email address.

## create_unicef_user

A pipeline function that creates a UNICEF user.

## user_details

A pipeline function that updates the user's details.
